/**
 * PRODUCTION NPCs
 *
 * Non-player characters for quests, trading, and interactions.
 * TODO: Extract NPCs from mission dialogs and create proper definitions.
 */

(function() {
'use strict';

const npcs = {
    // Stub - will be populated when NPC system is integrated
};

// Register NPCs (currently empty)
NPCRegistry.registerBatch(npcs, 'production');

console.log('✅ Loaded production NPCs (stub)');

})();
