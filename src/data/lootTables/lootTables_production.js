/**
 * PRODUCTION LOOT TABLES
 *
 * Drop tables for enemies, nodes, and chests.
 * TODO: Extract from enemy definitions and node resourceTables.
 */

(function() {
'use strict';

const lootTables = {
    // Stub - will be populated when loot system is refactored
};

// Register loot tables (currently empty)
LootTableRegistry.registerBatch(lootTables, 'production');

console.log('✅ Loaded production loot tables (stub)');

})();
